{
  "name": "image-processor",
  "version": "1.0.0",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "docker build -t lambda-sharp-builder . && docker run --rm -v $(pwd):/output lambda-sharp-builder"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "axios": "^1.8.2",
    "sharp": "^0.33.5"
  }
}
